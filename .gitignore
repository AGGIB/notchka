# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcscmblueprint
xcuserdata/
*.moved-aside

# Swift Package Manager
.build/
.swiftpm/
# Package.resolved НЕ игнорируется: это приложение, не библиотека — сборка
# обязана быть воспроизводимой, как только появится первая внешняя
# зависимость (GRDB.swift, KeyboardShortcuts, swift-snapshot-testing).

# Brainstorming companion (локальные мокапы сессии)
.superpowers/

# Артефакты сборки вендоренного адаптера
vendor/mediaremote-adapter/.build/

# XcodeGen генерирует проект из project.yml
Notchka.xcodeproj/

# Приёмка фундамента Notchka — 2026-08-10

Фундамент готов принять план 2. Все девять задач завершены и прошли ревью.
Проверка производительности и корректности завершена.

## Результаты проверки

### Шаг 1: Тесты

```bash
swift test --package-path Packages/NotchKit
```

**Результат: 32/32 тестов пройдено** ✓

Тесты покрывают:
- Геометрия чёлки (4)
- Машина состояний (14)
- Пороги курсора (6)
- Форма (5)
- Движение (3)

### Шаг 2: Релизная сборка

```bash
xcodebuild -project Notchka.xcodeproj -scheme Notchka \
  -configuration Release -derivedDataPath build build
```

**Результат: BUILD SUCCEEDED** ✓

**Предупреждений компилятора: 0** ✓

Проверка полного логи сборки (grep по `warning:` и `error:`):
- Ни одного сообщения вида `^s*warning:` (Swift compiler warnings)
- Завершение с `** BUILD SUCCEEDED **`

### Шаг 3: Расход в покое

Запуск: `open build/Build/Products/Release/Notchka.app`

Замер CPU над живым процессом, 20 образцов за 10 секунд (интервал 0.5s):

```
Sample  1: 0.0%
Sample  2: 0.0%
...
Sample 20: 0.0%
Среднее: 0.0%
```

**Результат: 0.0% на всех образцах** ✓

Процесс в покое не потребляет вычислений. Таймер не крутится.

## Ручные проверки

### Task 7 — панель-окно (проверено человеком)

Все пункты чек-листа пройдены:

- ✓ Панель видна поверх меню-бара
- ✓ Клик по меню активного приложения проходит насквозь
- ✓ Ввод продолжает попадать в активное приложение, фокус не украден
  (механизм подтверждён агентом: Notchka никогда не становится фронтальным приложением)
- ✓ Панель на месте при переключении Space
- ✓ В фуллскрине не мешает
- ✓ CPU около нуля в покое (подтверждено агентом: 0.0% на 20 образцах за 10 s)
- ✓ Силуэт совпадает с физическим вырезом (подтверждено числово агентом;
  человек заметил небольшой выступ на долю пункта с краёв и оценил как некритичное)

### Task 8 — курсор, хоткей, контроллер (проверено человеком)

Все пункты чек-листа пройдены:

- ✓ Наведение раскрывает панель пружиной, без рывка
- ✓ Быстрый проезд курсором мимо чёлки её не раскрывает
- ✓ `⌥Space` разворачивает панель и повторным нажатием сворачивает
- ✓ В развёрнутом состоянии уход курсора панель не закрывает
- ✓ Меню-бар кликается, когда панель закрыта
- ✓ CPU около нуля в покое (подтверждено агентом; подтверждено человеком вживую)

### Task 9 — Reduce Motion (проверено человеком)

- ✓ С включённым системным «Уменьшением движения» панель раскрывается
  затуханием без пружинного перелёта

## Осталось на потом

Накопленные наблюдения, не исправленные в ходе плана, сгруппированные по областям.

### Конфигурация и волшебные литералы

- **Task 2** (2 деferred items):
  - `CFBundleShortVersionString` и `CFBundleVersion` в `App/Info.plist:17-20`
    захардкожены как `1.0` и `1` вместо подстановок из project.yml.
    Версия приложения не растёт при бампе настроек, пока это не переписать.
  - Команда сборки не задаёт `-destination`, из-за чего xcodebuild печатает
    предупреждение о выборе первого из нескольких целевых назначений.

- **Task 7** (1 deferred item):
  - Размер панели `CGSize(width: 640, height: 260)` в `AppDelegate.swift:29`
    магическим литералом, вопреки правилу о хардкоде.

### Координаты и геометрия

- **Task 3** (1 deferred item):
  - Guard-условия в `NotchGeometry.swift:22,27` без комментариев,
    хотя кодируют основные правила модуля.

- **Task 7** (2 deferred items):
  - Силуэт панели выступает за физический вырез на доли пункта.
    Панель центруется по `screen.frame.midX`, а не по середине выреза.
    Точна только при равных боковых полосах меню-бара. В продакшене
    станет невидимым: настоящая панель чёрная поверх чёрной чёлки.
  - `ScreenMetricsReader.swift:11-12`: `?? 0` при nil боковых областей
    молча вырождает ширину выреза почти во весь экран вместо громкого отказа.

### Машина состояний и переходы

- **Task 4** (3 deferred items):
  - `NotchState.swift:10`: `?? .music` в `next` недостижим при непрерывных rawValue 1…4;
    при смене схемы молча подставит музыку вместо обнажения ошибки.
  - `NotchStateMachineTests.swift:89-95`: тест выхода из фуллскрина доказывает
    только половину инварианта порядка; вторую половину закрывает соседний тест.
  - Переход `peek(.trackChanged) + click` не покрыт отдельно, только как вариант
    другого входа того же события.

### Тестовое покрытие

- **Task 3** (1 deferred item):
  - `NotchGeometryTests.swift:23-27`: тест горячей зоны не проверяет `origin.y`.
    Риск низкий, унаследовано из плана.

- **Task 5** (1 deferred item):
  - Нет теста на `tick()` у свежего экземпляра до единого `cursorMoved`.
    Проверено вручную — безопасно возвращает nil.

- **Task 9** (1 deferred item + 1 припаркованная):
  - `NotchMotionTests.swift:22-27`: флаг Reduce Motion проверен только для
    `.expanded(.music)`; `.closed` и `.peek` под флагом не проверены.
  - **Припаркованная решением человека:** `NotchMotion.usesMorph(reduceMotion:)`
    написана и покрыта тестами, но потребителя не имеет. Требование спеки
    «морф заменяется кросс-фейдом» выполнено наполовину — переключается темп,
    форма продолжает перетекать. На отладочной заливке разница незаметна,
    проявится с настоящим содержимым вкладки в плане 2.

### Документация и комментарии

- **Task 1** (2 deferred items):
  - `note:26-27`: объяснение необходимости абсолютных путей подано как факт,
    хотя это рациональное предположение о `DynaLoader::dl_load_file`.
  - `note:43,143`: в синтаксисе осталась угловая скобка `<код>`.
    Реальные коды расписаны ниже.

- **Task 6** (1 deferred item):
  - `NotchShape.swift:58`: комментарий к правому вогнутому углу голая метка
    без причины, асимметричен левому. Влияние низкое.

- **Task 8** (2 deferred items):
  - `NotchController.swift:6-7`: doc-комментарий неточен для `peek(.trackChanged)`:
    автопик по смене трека включает мышь, когда курсор далеко от горячей зоны.
  - Диагностика отказа регистрации хоткея использует `NSLog`.
    Правила Swift пользователя предписывают `os.Logger` в продакшене.

- **Task 9** (1 deferred item):
  - Doc-комментарий `usesMorph` описывает поведение, которого пока нет ни в одной вьюхе.

### Прочее

- **Task 2** (1 deferred item, косметика):
  - `HotkeyCenter.register` около 43 строк, под лимитом 50, но близко.

- **Task 7** (1 deferred item):
  - `AppDelegate.swift:43`: повторный `setFrame` с той же рамкой,
    что уже передана в инициализатор.

- **Task 9** (1 deferred item, косметика):
  - `@Environment` объявлен между вычисляемыми свойствами, а не рядом
    с хранимыми.

---

## Вывод

Фундамент прошёл все проверки:

- ✓ Целевое число тестов достигнуто: 32/32
- ✓ Релизная сборка чиста: BUILD SUCCEEDED, 0 предупреждений
- ✓ Процесс эффективен в покое: 0.0% CPU на всех образцах
- ✓ Ручные чек-листы Tasks 7–9 полностью закрыты

Накопленные нерешённые вопросы записаны выше. Большинство относятся
к категориям, которые перейдут на план 2 низким приоритетом: косметика
(комментарии, расстановка, комбинирование раздельных тестов), известные
отложенные потребности (морфинг), и безопасные по анализу конструкции
(guard независимо от состояния, `?? .music` недостижим).

Планы 2–4 могут опираться на эту заметку как на источник истины по готовности
фундамента к расширению.
